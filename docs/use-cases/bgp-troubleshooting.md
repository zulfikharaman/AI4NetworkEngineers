# BGP Troubleshooting Using AI

This use case demonstrates **how a Network Engineer can safely use AI**
to analyze BGP issues **without blindly trusting the output**.

---

## 🔴 Problem Statement

A BGP neighbor is stuck in `Idle / Active / Connect` state.
The session does not come up even though configuration looks correct.

---

## 📌 Sample CLI Output

```text
BGP neighbor is 10.1.1.2, remote AS 65002
  BGP state = Active
  Last error: Connection timed out
